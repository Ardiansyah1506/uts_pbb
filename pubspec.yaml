name: uts_pbb
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.3.2
  url_launcher: ^6.1.5  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.10.0  # Add the correct package and version

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/logo.jpg"  # Replace with your icon path

flutter:
  uses-material-design: true
  assets:
    - assets/ # Corrected 'asssets' to 'assets'
